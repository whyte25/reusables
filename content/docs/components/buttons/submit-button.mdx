---
title: Submit Button
description: Submit button to indicate loading state.
---

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
  ```package-install
npx shadcn@latest add "https://reusables.vercel.app/r/submit-button"
```
</Tab>

<Tab value="Manual">

<Step>
Copy and paste the following code into your `submit-button.tsx` file.
<CollapsibleCode>
```json doc-gen:file
{
  "file": "registry/default/reusables/ui/submit-button.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"submit-button.tsx\""
  }
}
```
</CollapsibleCode>  
</Step>

<Step>
Create and copy the following code into your `Spinner.tsx` file:
<CollapsibleCode>
```json doc-gen:file
{
  "file": "registry/default/reusables/ui/spinner.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"spinner.tsx\""
  }
}
```
</CollapsibleCode>
</Step>

<Step>
Create and copy the following code into your `lib` folder:
<CollapsibleCode>
```json doc-gen:file
{
  "file": "registry/default/lib/utils.ts",
  "codeblock": {
    "lang": "ts",
    "meta": "title=\"utils.ts\""
  }
} 
```
</CollapsibleCode>
</Step>

</Tab>

</Tabs>

## Usage

```tsx
import { SubmitButton } from "@/components/ui/submit-button"
```

```tsx
<SubmitButton isSubmitting={isSubmitting}> Loading </SubmitButton>
```

## Examples

### Default

<ComponentSource name="submit-button-demo" reTrigger={false} />

### Position Left

<ComponentSource name="submit-button-position-left" reTrigger={false} />

### Hide Text

<ComponentSource name="submit-button-hidetext-demo" />

## Reference

<TypeTable
  type={{
    value: {
      children: "",
      type: "ReactNode",
      default: "",
    },
    hideText: {
      description: "hide the text and show only the spinner",
      type: "boolean",
      default: "false",
    },
    className: {
      description: "Class name to style the button",
      type: "string",
      default: "",
    },
    spinnerClassName: {
      description: "Class name to style the spinner",
      type: "string",
      default: "",
    },
    spinnerSize: {
      description: "Spinner size",
      type: "default | sm | lg | icon",
      default: "default",
    },
    position: {
      description: "Spinner position",
      type: "right | left",
      default: "right",
    },
  }}
/>
