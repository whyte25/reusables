---
title: Copy Button
description: A button component to copy text to clipboard
---

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
  ```package-install
npx shadcn@latest add "https://reusables.vercel.app/r/copy-button"
```
</Tab>

<Tab value="Manual">
<Steps> 
<Step>
Copy and paste the following code into your `copy-button.tsx` file.
<CollapsibleCode>
```json doc-gen:file
{
  "file": "registry/default/reusables/ui/copy-button.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"copy-button.tsx\""
  }
}
```
</CollapsibleCode>
</Step>

<Step>
Create and copy the following code into your `lib` folder:
<CollapsibleCode>
```json doc-gen:file
{
  "file": "registry/default/lib/utils.ts",
  "codeblock": {
    "lang": "ts",
    "meta": "title=\"utils.ts\""
  }
}
```
</CollapsibleCode>
</Step>

</Steps>
</Tab>

</Tabs>

## Usage

```tsx
import CopyButton from "@/components/ui/copy-button"
```

```tsx
const Example = () => {
  return (
    <div>
      <CopyButton value="Text to copy" />
    </div>
  )
}
```

## Example

<ComponentSource name="copy-button-demo" reTrigger={false} />

## Reference

<TypeTable
  type={{
    value: {
      description: "The value to be copy",
      type: "string",
      default: "",
    },
    className: {
      description: "Class name to style the button",
      type: "string",
      default: "",
    },
    variant: {
      description: "The button style variant",
      type: "default | outline | ghost",
      default: "ghost",
    },
  }}
/>
