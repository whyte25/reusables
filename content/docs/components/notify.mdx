---
title: Notify
description: A toast notification system with customizable positioning and styling, inspired by Sanity UI's Toast.
---

## Installation

<Tabs items={['CLI', 'Manual']}>

  <Tab value="CLI">
    ```package-install
npx shadcn@latest add "https://reusables.vercel.app/r/notify"
````
</Tab> 
<Tab value="Manual">
Create a `notify` folder in your `ui` folder.
<Steps>

<Step>

Copy and paste this code into your `notify-provider.tsx` file.

<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/notify-provider.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"notify-provider.tsx\""
  }
}
```
</CodeBlockWrapper>

</Step>
<Step>
Copy and paste this code into your `notify.tsx` file.
<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/notify.tsx",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"notify.tsx\""
  }
}
```
</CodeBlockWrapper>
</Step>

<Step>
Copy and paste this code into your `notify-animations.ts` file.
<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/notify-animations.ts",
  "codeblock": {
    "lang": "ts",
    "meta": "title=\"notify-animations.ts\""
  }
}
```
</CodeBlockWrapper>
</Step>

<Step>
Copy and paste this code into your `notify-config.ts` file.
<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/notify-config.ts",
  "codeblock": {
    "lang": "ts",
    "meta": "title=\"notify-config.ts\""
  }
}
```
</CodeBlockWrapper>
</Step>

<Step>
Copy and paste this code into your `notify-state-manager.ts` file.
<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/notify-state-manager.ts",
  "codeblock": {
    "lang": "ts",
    "meta": "title=\"notify-state-manager.ts\""
  }
}
```
</CodeBlockWrapper>
</Step>

<Step>
Copy and paste this code into your `notify-types.ts` file.
<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/notify-types.ts",
  "codeblock": {
    "lang": "ts",
    "meta": "title=\"notify-types.ts\""
  }
}
```
</CodeBlockWrapper>
</Step>

<Step>
Copy and paste this code into your `notify-variants.ts` file.
<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/notify-variants.ts",
  "codeblock": {
    "lang": "ts",
    "meta": "title=\"notify-variants.ts\""
  }
}
```
</CodeBlockWrapper>
</Step>

<Step>
Copy and paste this code into your `notify-utils.ts` file.
<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/notify-utils.ts",
  "codeblock": {
    "lang": "ts",
    "meta": "title=\"notify-utils.ts\""
  }
}
```
</CodeBlockWrapper>
</Step>

<Step>
Copy and paste this code into your `index.ts` file.
<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/index.ts",
  "codeblock": {
    "lang": "ts",
    "meta": "title=\"index.ts\""
  }
}
```
</CodeBlockWrapper>
</Step>

<Step>
Copy and paste this code into your `notify-utils.ts` file.
<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/notify-utils.ts",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"notify-utils.ts\""
  }
}
```
</CodeBlockWrapper>

</Step>

<Step>
Copy and paste this code into your `index.ts` file.
<CodeBlockWrapper>
```json doc-gen:file
{
  "file": "registry/reusables/ui/notify/index.ts",
  "codeblock": {
    "lang": "tsx",
    "meta": "title=\"index.ts\""
  }
}
```
</CodeBlockWrapper>

</Step>

<Step>
Create and copy the following code  into `utils` file in your `lib` folder:

<CodeBlockWrapper>

```json doc-gen:file
{
  "file": "registry/lib/utils.ts",
  "codeblock": {
    "lang": "ts",
    "meta": "title=\"utils.ts\""
  }
}
```

</CodeBlockWrapper>

</Step>

</Steps>
</Tab> 
</Tabs>

Add the Toast Provider to your layout

<Tabs items={["Next.js", "React"]}>
  <Tab value="Next.js">
  ```tsx title="layout.tsx"
import { ToastProvider } from "@/components/ui/notify";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <ToastProvider>{children}</ToastProvider>
      </body>
    </html>
  );
}
  ```
  </Tab> <Tab value="React">
  ```tsx title="src/app.tsx"
import { ToastProvider } from "@/components/ui/notify";

function App() {
return (

 <ToastProviders>
 <YourApp />
 </ToastProviders>
);
}
```
</Tab> 
</Tabs>

## Usage

```tsx
import { toast } from "@/components/ui/notify"
```

```tsx
toast.info("Event has been created")
```

## Examples

### Basic Usage

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-demo"
/>

### Interactive Toasts

Toasts are interactive by default. You can pause the dismiss timer and progress bar animation by hovering over a toast, and dismiss it by dragging it on either the horizontal or vertical axis.

### Toast With JSX

Pass JSX directly to the toast function to render rich content.

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-with-jsx-demo"
/>

### Toast With Action

Add rich, interactive actions to your toasts. Configure a primary action and a dismiss button, with support for both text and icon variants.

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-with-action-demo"
/>

### Toast With Description

Enhance toast messages with additional details.

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-with-description-demo"
/>

### Toast Positions

Control where toasts appear on the screen.

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-positions-demo"
/>

### Animations

Customize the entrance and exit animations for toasts.

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-animations-demo"
/>

### Progress Bar

Control the visibility of the countdown indicator.

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-progress-demo"
/>

### Close Button

Configure manual dismissal options.

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-close-button-demo"
/>

### Prevent Duplicates

Enable `preventDuplicates` to avoid displaying toasts with identical messages more than once.

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-prevent-duplicates-demo"
/>

### Max Toasts

Limit the number of visible toasts.

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-custom-config-demo"
/>

### Promise Handling

The `toast.promise` method provides a convenient way to display toast notifications for asynchronous operations.

<ComponentSource
  showV0Button={false}
  reTrigger={false}
  className="md:px-0"
  name="toast-promise-demo"
/>

## Toast Provider

Override default toast behavior using the ToastProvider.

```tsx
<ToastProvider
  position="bottom-right"
  animation="slide"
  duration={4000}
  closable={true}
  classNames={{
    error: "bg-red-100 text-red-900",
    success: "bg-green-100 text-green-900",
    title: "font-bold",
    description: "text-sm",
    closeButton: "hover:bg-gray-200",
    containerClassName: "gap-2",
  }}
  preventDuplicates={true}
  maxToast={4}
>
  {children}
</ToastProvider>
```

## Toast Push Method

For complete control over toast configuration, use the `toast.push` method:

```tsx
toast.push({
  text: "Custom Toast",
  status: "info", // 'error' | 'warning' | 'success' | 'info' | 'default' | 'loading'
  position: "top-right",
  animation: "bounce",
  duration: 5000,
  description: "This is a custom toast",
  closable: true,
  hideProgressBar: false,
  classNames: {
    title: "font-bold",
    description: "text-sm",
    closeButton: "hover:bg-gray-200",
  },
})
```

## Reference

### ToastProvider Props

<TypeTable
  type={{
    position: {
      type: '"top-left" | "top-center" | "top-right" | "bottom-left" | "bottom-center" | "bottom-right"',
      default: '"bottom-right"',
      description: "Default position for toasts",
    },
    animation: {
      type: '"slide" | "fade" | "scale" | "bounce"',
      default: '"slide"',
      description: "Default animation for toasts",
    },
    duration: {
      type: "number",
      default: "4000",
      description: "Duration in milliseconds before toast auto-dismisses",
    },
    closable: {
      type: "boolean",
      default: "true",
      description: "Whether toasts can be manually closed",
    },
    classNames: {
      type: "ToastClassNames",
      description: "Object containing class names for styling",
    },
    preventDuplicates: {
      type: "boolean",
      default: "true",
      description: "Prevent duplicate toast messages globally",
    },
    maxToast: {
      type: "number",
      default: "4",
      description: "Maximum number of toasts shown at once globally",
    },
    animation: {
      type: '"slide" | "fade" | "scale" | "bounce"',
      default: '"slide"',
      description: "Default animation style for all toasts",
    },
  }}
/>

### Toast Methods

<TypeTable
  type={{
    "toast.success": {
      type: "(text: React.ReactNode, options?: ToastParams) => string",
      description: "Display a success toast notification",
    },
    "toast.error": {
      type: "(text: React.ReactNode, options?: ToastParams) => string",
      description: "Display an error toast notification",
    },
    "toast.warning": {
      type: "(text: React.ReactNode, options?: ToastParams) => string",
      description: "Display a warning toast notification",
    },
    "toast.info": {
      type: "(text: React.ReactNode, options?: ToastParams) => string",
      description: "Display an info toast notification",
    },
    "toast.loading": {
      type: "(text: React.ReactNode, options?: ToastParams) => string",
      description: "Display a loading toast notification",
    },
    "toast.default": {
      type: "(text: React.ReactNode, options?: ToastParams) => string",
      description: "Display a default toast notification",
    },
    "toast.push": {
      type: "(params: ToastParams) => string",
      description: "Create a custom toast with full configuration control",
    },
    "toast.promise": {
      type: "<T>(promise: () => Promise<T>, options: ToastPromiseOptions<T>) => string",
      description:
        "Handle async operations with loading, success, and error states",
    },
  }}
/>

### Toast Params

<TypeTable
  type={{
    text: {
      type: "React.ReactNode",
      required: true,
      description: "Main message of the toast, can be a string or JSX.",
    },
    animation: {
      type: '"slide" | "fade" | "scale" | "bounce"',
      description:
        "Overrides the default animation for a specific toast. Each animation has distinct characteristics: slide (moves in from the side), fade (simple opacity transition), scale (grows/shrinks with opacity), and bounce (spring physics for playful effect).",
    },
    actions: {
      type: "{ primary: Action; dismiss?: boolean | Omit<Action, 'icon' | 'variant'> }",
      description:
        "Add interactive actions. The primary action can be a button or icon. The dismiss action can be a boolean (to show a default dismiss button) or a custom action object.",
    },
    description: {
      type: "React.ReactNode",
      description: "Additional details below the title",
    },
    status: {
      type: '"error" | "warning" | "success" | "info" | "default" | "loading"',
      default: '"default"',
      description: "Visual style of the toast",
    },
    duration: {
      type: "number",
      default: '"4000"',
      description: "Override default duration for this toast",
    },
    position: {
      type: "ToastPosition",
      description: "Override default position for this toast",
    },
    preventDuplicates: {
      type: "boolean",
      description: "Override global duplicate prevention for this toast",
    },
    maxToast: {
      type: "number",
      description: "Override global maximum toast count for this toast",
    },
    hideProgressBar: {
      type: "boolean",
      default: "false",
      description: "Hide the progress bar for this toast",
    },
  }}
/>
